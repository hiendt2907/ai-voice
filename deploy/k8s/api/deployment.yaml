apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: ai-voice
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      imagePullSecrets:
        - name: harbor-ai-voice-pull
      containers:
        - name: api
          image: harbor.harbor.svc.cluster.local/ai-voice/api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: ai-voice-config
            - secretRef:
                name: ai-voice-secrets
            - secretRef:
                name: voip24h-secret
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
