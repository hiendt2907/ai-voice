apiVersion: apps/v1
kind: Deployment
metadata:
  name: voice
  namespace: ai-voice
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: voice
  template:
    metadata:
      labels:
        app: voice
    spec:
      imagePullSecrets:
        - name: harbor-ai-voice-pull
      containers:
        - name: voice
          image: harbor.harbor.svc.cluster.local/ai-voice/voice:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ai-voice-config
            - secretRef:
                name: ai-voice-secrets
            - secretRef:
                # Standalone secret (NOT Vault/ExternalSecret-managed like
                # ai-voice-secrets above — created directly via kubectl,
                # edited via Lens). Holds XKIRO_API_KEY for the xKiro TTS
                # evaluation adapter (tts/xkiro_tts.py).
                name: ai-voice-xkiro
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            limits:
              cpu: 2000m
              memory: 2Gi
