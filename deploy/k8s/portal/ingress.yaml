apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portal
  namespace: ai-voice
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: ai-voice-https-redirect@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: aivoice.asia
      http:
        paths:
          # Portal Simulator's browser-side WS clients (same-origin, avoids
          # needing voice worker exposed anywhere) — see apps/portal/ws-proxy.js.
          # Covers both /ws/call (drives a call) and /ws/watch/:phone
          # (read-only live transcript fan-out for the voip24h dial feature).
          - path: /ws
            pathType: Prefix
            backend:
              service:
                name: portal
                port:
                  number: 3001
          - path: /
            pathType: Prefix
            backend:
              service:
                name: portal
                port:
                  number: 3000
  tls:
    - hosts:
        - aivoice.asia
      secretName: aivoice-asia-tls
