apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: ai-voice
spec:
  selector:
    app: portal
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: ws-proxy
      port: 3001
      targetPort: 3001
